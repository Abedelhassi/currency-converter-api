# CVE-2025-47273: setuptools vendored inside pip (build-time tool).
# Not exploitable at runtime; we don't use setuptools PackageIndex.
CVE-2025-47273

# GHSA-6v7p-g79w-8964: msgpack vendored inside another package.
# Our direct dependency (msgpack 1.2.2) is patched. This is a transitive vendored copy.
GHSA-6v7p-g79w-8964